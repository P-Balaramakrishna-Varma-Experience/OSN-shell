#ifndef __System_M 
#define __System_M


#include "../Data_Abstractions/StringB.h"

//It is called by Built Manager. Input is a commoand (may or maybe not valid)
// tries Runs the command  (calling the foreground or background) in appropriate mode
void Distributor_2(String Command);


#endif